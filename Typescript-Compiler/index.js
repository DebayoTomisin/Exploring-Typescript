"use strict";
console.log('The typescript compiler');
// to automate the process of recompiling the script, we enter watch mode and the we do that by using the regular command followed by --watch or -w
"in a case where anything where you want to run the entire project folder, we make use  of tsc --init and this initialzies the entire folder, and after we run tsc and that compiles all the ts files ";
console.log('watch mode, send data');
let data = 'hello there';
const files = 90;
